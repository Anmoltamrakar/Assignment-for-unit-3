/* Q,3. Write a program that declares an integer variable call num, assigns a value to it,
    and computes and prints out the value of the variable itself,
    its square adn its cube. */

#include<stdio.h>

 int main()

 {
     int num;
     num = 15;
     printf("\n\n\t\t Square of number %d is %d",num,num*num);
     printf("\n\n\t\t Square of number %d is %d \n\n\n",num,num*num*num);


     return 0;
 }

